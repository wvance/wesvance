<%= form_for(@post_attachment, :html => { :multipart => true }) do |f| %>
  <% if @post_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_attachment.errors.count, "error") %> prohibited this post_attachment from being saved:</h2>

      <ul>
      <% @post_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= image_tag(@post_attachment.image, :width => 300)%>
  <%= form_for(@post_attachment) do |f| %>
    <div class="field">
      <%= f.label :image %><br>
      <%= f.file_field :image %>
    </div>
    <div class="field">
      <%= f.label :post_id %><br>
      <%= f.number_field :post_id %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

  
<% end %>
